<div class="row">
  <div class="col-md-12">
    <div class="card card-plain">
      <div class="card-header card-header-primary">
        <h4 class="card-title mt-0"><%= t ".manage_artists" %></h4>
        <p class="card-category"><%= t ".sub_artists" %></p>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <%= render "new" %>
          <%= render "search" %>
          <%= paginate @artists %>
          <table class="table table-hover">
            <thead>
              <th class="width_avatar"><%= t ".avatar" %></th>
              <th class="width_name">
                <%= sort_link @search, :name, t(".name") %>
              </th>
              <th class="width_description">
                <%= sort_link @search, :description, t(".description") %>
              </th>
              <th>albums</th>
              <th>songs</th>
              <th class="width_head_action"><%= t ".action" %></th>
            </thead>
          </table>
          <%= render "not_found" %>
          <%= render "artists" %>
        </div>
      </div>
    </div>
  </div>
</div>
