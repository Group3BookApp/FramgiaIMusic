<div class="card card-plain">
  <div class="dark-edition">
    <div class="card-header card-header-primary">
      <h4 class="modal-title"><%= t ".view_category" %></h4>
    </div><br>
    <div class="modal-body">
      <div class="card card-profile">
        <div class="card-avatar">
          <%= image_tag @category.avatar.url,size: Settings.size_avatar %>
        </div>
        <div class="card-body">
          <h4 class="card-title"><%= @category.name %></h4>
          <p class="card-description"><%= @category.description %></p>
        </div>
      </div>
    </div>
    <%= link_to t(".backhome"), admin_categories_path, class: "btn btn-info" %>
    <%= link_to t(".edit"), edit_admin_category_path(@category),
      class: "width_a btn btn-warning" %>
    <%= link_to t(".delete"), admin_category_path(@category), method: :delete,
      class: "width_a btn btn-danger", data: {confirm: t(".you_sure")} %>
  </div>
</div>
